# 2026.251.1508 — fix(pin): pod-postgresql v2026.239.1627 → v2026.247.1304 (the stale-pin class — multi-version resolution warnings)

## Summary

The pod-postgresql v2026.239.1627 pin is the v2026.239.x stale-pin class (R1): every check bed run emits `candy github.com/opencharly/pod-postgresql resolved to multiple versions; using newest 2026.247.0100 (from @v2026.247.1304), ignoring 2026.144.1531 (from @v2026.239.1627)` because this layer still requests the old tag while the resolver picks the newest. Same family the android sweep fixed component-by-component.

This PR aligns the pin to the newest merged tag v2026.247.1304.

## How tested

## How tested

**Resolution proof (the branch overridden into the composing cachyos project):**

Before (this layer at the OLD pin, pre-fix):

```
$ charly box validate
warning: Warning: candy github.com/opencharly/pod-postgresql resolved to multiple versions; using newest 2026.247.0100 (from github.com/opencharly/pod-postgresql@v2026.247.1304), ignoring 2026.144.1531 (from github.com/opencharly/pod-postgresql@v2026.239.1627)
charly box validate: OK — checked 506 candies, 40 boxes, 45 deploys, 7 distros, 7 builders; 1 warning, 0 errors
```

After (this branch overridden):

```
$ CHARLY_REPO_OVERRIDE="github.com/opencharly/distro-cachyos=…,github.com/opencharly/layer-vectorchord=…,github.com/opencharly/pod-immich=…" charly box validate
charly box validate: OK — checked 506 candies, 40 boxes, 45 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
```

The multi-version notice is gone — this layer's pin was the last remaining v2026.239.1627 requester in the cachyos closure (pod-immich's bump rides the companion PR). Live execution of the aligned pin: the E-5 check-android-emulator-pod re-run (distro-cachyos #80) composes this layer; its exit-0 calver + matrix + evidence are pasted there.

**Live execution (this layer's aligned pin in the E-5 bed composition):** the check-android-emulator-pod re-run on the E-5 final head — calver **2026.251.1443**, exit 0, ok:true, **15/15 phases** — resolved this layer's branch via CHARLY_REPO_OVERRIDE (the same content this PR ships) at **ZERO non-allowlisted warnings** (run diagnostics: errors 0, only 17 allowlisted dev-worktree-binary-notice; the pod-postgresql multi-version notice is gone). Gate rows:

    evidence:
      - instrument: check-android-emulator-pod.appium-screen
        origin: session
        verb: appium
        venue: check-android-emulator-pod
        phase: live
        segment:
          - { bytes: 87153918, phase: "live", start: "2026-09-08T14:46:59Z", stop: "2026-09-08T14:53:17Z" }
        artifact:
          - { kind: "mp4", path: ".check/check-android-emulator-pod/2026.251.1443/media/appium-1.mp4" }
        pipeline:
          - plugin: transcode
            plugin_input: { artifact: "/tmp/charly-appium-screen.mp4", to: "mp4" }

Full matrix + evidence live on opencharly/distro-cachyos #80 (the E-5 gate PR).

## Rulebook compliance

- R1 — root-caused: the old pin is the v2026.239.x stale-pin class; aligned to the newest merged tag.
- R3 — one implementation (the pod-postgresql require); no duplication.
- R4 — no workarounds; the pin is a merged ref.
- R6 — no force-push; feat/ branch + PR-only landing.

## Change classification

- **Change class**: pin bump (config, charly.yml).
- **Verification gate**: resolution validation (box validate 0 warnings with the branch overridden) + live execution in the E-5 bed re-run.
- **Attribution tier**: analysed on a live system — the resolution proof (before/after, 1 warning → 0) and the live E-5 run 2026.251.1443 (exit 0, 15/15 phases, ZERO non-allowlisted warnings, gate rows) are both pasted in How tested; the run executed this layer's aligned pin via override. The CI `build`/candy check fails only because the repo's CI workflow pins a charly whose max schema (2026.232.0520) predates pod-postgresql v2026.247.1304's schema (2026.240.1943). **Named exit (B14, not parking):** the org charly-pin refresh batch — the umbrella pin wave alongside opencharly/distro-cachyos #80 — re-pins each repo's CI charly to a schema ≥ 2026.240.1943; CI is not this repo's merge gate (`validate / validate` governs) but the failure is tracked to that batch.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*